#set("content") {
	<div class="container">
		#set("pageTitle") { Customers }
		#embed("includes/page-header")

		<div class="row row-cards row-deck">
			<div class="col-12">
				#embed("cards/customers.leaf")
			</div>
		</div>
	</div>
}

#embed("layouts/default")
