#set("content") {
	<div class="container">
		#embed("includes/page-header")

		<div class="row row-cards row-deck">
			<div class="col-12">
				#set("dispatchCardTitle") {Dispatches}
				#embed("cards/dispatches.leaf")
			</div>
		</div>
	</div>
}

#embed("layouts/default")
