<div class="card">
	<div class="card-header">
	<h3 class="card-title">#if(mapCardName) {#get(mapCardName)}else{Map}</h3>
	</div>
	<div class="card-body">
		<div class="card-map" id="map"></div>
	</div>
</div>

#embed("js-mapbox")
